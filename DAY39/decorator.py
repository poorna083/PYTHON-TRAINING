# Decorator:
# A decorator is a function that adds extra functionality to another function without modifying its original code.
# In Python, functions are treated as objects, so a function can be:
# Assigned to a variable
# Passed as an argument
# Returned from another function
# Decorators use this feature to wrap another function

# def deco(func):
#     def inner(x,y):
#         print("You got :",end='')
#         func(x,y)
#     return inner
# @deco
# def sample(a,b):
#     print(a+b)
# sample(3,4)

