# split():
#      it is used to split the given string based on the saperator and maximum splits
#      the return type of split is list



# syntax:
# var_name.split([saperator],[max_splits])

s="hello every one"
print(s.split())
print(s.split(maxsplit=1))
print(s.split('e'))
